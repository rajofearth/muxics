// Pre-generated visualizer bar data for fallback when analyser is unavailable
export const VISUALIZER_DATA = Array.from({ length: 120 }).map(() => ({
  delay: Math.random() * -2,
  duration: 0.5 + Math.random() * 0.8,
  height: 20 + Math.random() * 80,
}));
